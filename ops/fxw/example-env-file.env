# Example server-only environment file for FXW/Linux staging.
# Store the real file only on the server, for example:
# /etc/gemeente-ai-assistent.env
#
# Do not commit a real API key.
# If OPENAI_API_KEY is missing, the app runs in mock mode.

PORT=3000
OPENAI_API_KEY=replace_on_server_only
